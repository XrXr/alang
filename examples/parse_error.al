main :: proc() {
	clearly wrong
	mystruct.234
	hello =
	today :=
	// using unary operator as binary operator
	a := using @ unary

	meow := "this string is unmatched
	call_unmatched(sdfsdf, sdf(, sdf)
}
